import { Meta, Story } from '@storybook/addon-docs/blocks';
import {
  DocLayout,
  Canvas,
  PropsTable,
  parseSubcomponents,
} from '../../../.storybook/components';
import * as SelectStories from './select.stories';

<Meta of={SelectStories} />

<DocLayout meta={SelectStories.default}>
  ## Default Example

<Canvas of={SelectStories.Default} background="dots" />

## Component API

### Select

A versatile select component built on top of Radix UI's Select primitive. Provides a customizable dropdown selection interface with support for grouping, labeling, and scrolling through large lists of options.

<PropsTable argTypes={SelectStories.default.argTypes} />

{(
() => {
const subcomponents = parseSubcomponents(SelectStories.default.subcomponents);
return subcomponents.map((sub) => (

<div key={sub.name}>
  <h3 className="sb-unstyled text-foreground mt-6 mb-3 text-xl font-bold">
    {sub.name}
  </h3>
  {sub.description && (
    <p className="sb-unstyled text-muted-foreground mb-4 text-base">
      {sub.description}
    </p>
  )}
  <PropsTable
    title={`${sub.name} Props`}
    argTypes={SelectStories.default.subcomponents[sub.name].argTypes}
  />
</div>
)); }) ()}

## Examples

### With Groups

Organize options into logical groups with labels and separators.

<Canvas of={SelectStories.WithGroups} background="grid" />

### With Many Options

Handle large lists of options with built-in scrolling and search capabilities.

<Canvas of={SelectStories.WithManyOptions} background="muted" />

### Small Size

Use the small size variant for compact interfaces and dense layouts.

<Canvas of={SelectStories.SmallSize} background="card" />

### Disabled

Show how selects behave when disabled entirely.

<Canvas of={SelectStories.Disabled} background="transparent" />

### With Disabled Items

Individual select items can be disabled while keeping others interactive.

<Canvas of={SelectStories.WithDisabledItems} background="default" />

### Controlled

Manage the select value with React state for form integration.

<Canvas of={SelectStories.Controlled} background="grid" />

### RTL

Support for right-to-left languages and reading directions.

<Canvas of={SelectStories.RTL} background="muted" />

## Accessibility

The `Select` component follows the [WAI-ARIA Listbox Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) and includes:

- Full keyboard navigation with arrow keys, Enter, and Escape
- Screen reader support with proper ARIA attributes
- Focus management when opening and closing
- Type-ahead functionality for quick option selection
- Group and option announcements for screen readers

## Best Practices

- Provide clear, descriptive placeholder text
- Use logical grouping for long option lists
- Keep option labels concise but descriptive
- Use disabled states to communicate unavailable options
- Test with keyboard navigation and screen readers
- Consider using search functionality for very long lists

</DocLayout>