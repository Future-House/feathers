import { Meta, Story } from '@storybook/addon-docs/blocks';
import {
  DocLayout,
  Canvas,
  PropsTable,
  parseSubcomponents,
} from '../../../.storybook/components';
import * as TooltipStories from './tooltip.stories';

<Meta of={TooltipStories} />

<DocLayout meta={TooltipStories.default}>
  ## Default Example

<Canvas of={TooltipStories.Default} background="dots" />

## Component API

### Tooltip

A tooltip component built on top of Radix UI Tooltip primitive. Provides contextual information when users hover or focus on an element.

<PropsTable argTypes={TooltipStories.default.argTypes} />

## Examples

### Positioning

Control tooltip placement around the trigger element.

<Canvas of={TooltipStories.Positioning} background="grid" />

### Alignment

Fine-tune tooltip alignment relative to the trigger.

<Canvas of={TooltipStories.Alignment} background="muted" />

### With Offset

Add custom spacing between tooltip and trigger.

<Canvas of={TooltipStories.WithOffset} background="card" />

### With Delay

Control the delay before tooltip appears using TooltipProvider.

<Canvas of={TooltipStories.WithDelay} background="transparent" />

### Controlled Tooltip

Programmatically control tooltip visibility.

<Canvas of={TooltipStories.Controlled} background="default" />

### Rich Content

Tooltips can contain complex content beyond simple text.

<Canvas of={TooltipStories.RichContent} background="grid" />

### Different Triggers

Various elements that can trigger tooltip displays.

<Canvas of={TooltipStories.DifferentTriggers} background="muted" />

## Accessibility

The `Tooltip` component follows the [WAI-ARIA Tooltip Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/) and includes:

- Keyboard focus support with Escape key to close
- Screen reader compatibility with proper ARIA attributes
- Focus management that doesn't trap keyboard users
- Support for both hover and focus interactions
- Automatic collision detection and repositioning

## Best Practices

- Keep tooltip content concise and helpful
- Use tooltips for supplementary information, not essential content
- Ensure tooltips don't interfere with user interactions
- Test with keyboard navigation and screen readers
- Consider mobile users who cannot hover
- Use appropriate delays to avoid overwhelming users
- Position tooltips to avoid covering important content
- Provide alternative ways to access tooltip information on touch devices

</DocLayout>