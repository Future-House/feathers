import { Meta, Story } from '@storybook/addon-docs/blocks';
import {
  DocLayout,
  Canvas,
  PropsTable,
  SubcomponentPropTable,
} from '../../../.storybook/components';
import * as NavigationMenuStories from './navigation-menu.stories';

<Meta of={NavigationMenuStories} />

<DocLayout meta={NavigationMenuStories.default}>

## Default Example

<Canvas of={NavigationMenuStories.Default} />

## Component API

### NavigationMenu

<PropsTable argTypes={NavigationMenuStories.default.argTypes} />

<SubcomponentPropTable
  subcomponents={NavigationMenuStories.default.subcomponents}
/>

## Examples

### Product Navigation

Navigation menu designed for product websites with rich content previews.

<Canvas of={NavigationMenuStories.ProductNavigation} />

### Simple Links

Basic navigation with simple link items for minimal interfaces.

<Canvas of={NavigationMenuStories.SimpleLinks} />

### Vertical Orientation

Navigation menu configured for vertical layouts, perfect for sidebars.

<Canvas of={NavigationMenuStories.VerticalOrientation} />

### Without Viewport

Navigation menu without the viewport component for custom positioning.

<Canvas of={NavigationMenuStories.WithoutViewport} />

### With Delay Duration

Customize the hover delays for different interaction patterns.

<Canvas of={NavigationMenuStories.WithDelayDuration} />

## Accessibility

The `NavigationMenu` component follows the [WAI-ARIA Navigation Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/navigation/) and includes:

- Keyboard navigation with Tab, arrow keys, Enter, and Escape
- Screen reader support with proper ARIA attributes
- Focus management when navigating between items
- Support for both horizontal and vertical orientations
- Automatic viewport positioning to prevent content overflow

## Best Practices

- Use clear, descriptive link text that makes sense out of context
- Group related navigation items logically
- Consider the visual hierarchy when designing menu content
- Test with keyboard navigation to ensure smooth interactions
- Use appropriate delay timing based on your users' needs
- Provide alternative navigation methods for complex structures

</DocLayout>
