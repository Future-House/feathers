import { Meta, Story } from '@storybook/addon-docs/blocks';
import {
  DocLayout,
  Canvas,
  PropsTable,
  SubcomponentPropTable,
} from '../../../.storybook/components';
import { Badge } from '../badge/badge';
import * as CarouselStories from './carousel.stories';

<Meta of={CarouselStories} />

<DocLayout meta={CarouselStories.default} hideRadixUiUrl >

## Default Example

<Canvas of={CarouselStories.Default} />

## Component API

### Carousel

You may need to reference the official documentation for the [embla-carousel library](https://www.embla-carousel.com/api/) to understand the full API and available options.

<PropsTable argTypes={CarouselStories.default.argTypes} />

<SubcomponentPropTable subcomponents={CarouselStories.default.subcomponents} />

## Examples

### Vertical Orientation

Carousel with vertical scrolling direction.

<Canvas of={CarouselStories.Vertical} />

### Multiple Items Per View

Show multiple carousel items at once with partial visibility.

<Canvas of={CarouselStories.MultipleItemsPerView} />

### With Custom Content

Carousel featuring rich content with gradients and detailed information.

<Canvas of={CarouselStories.WithCustomContent} />

### Image Carousel

Carousel optimized for displaying images with looping enabled.

<Canvas of={CarouselStories.ImageCarousel} />

### Without Controls

Carousel without navigation arrows for auto-scrolling or gesture-only navigation.

<Canvas of={CarouselStories.WithoutControls} />

### Custom Sizing

Carousel with custom item sizing showing multiple items.

<Canvas of={CarouselStories.CustomSizing} />

## Accessibility

The `Carousel` component follows accessibility best practices:

- Supports keyboard navigation with Tab, Enter, and arrow keys
- Provides proper ARIA labels and roles for screen readers
- Maintains focus management when navigating between items
- Includes proper semantic structure for carousel content
- Supports screen reader announcements for slide changes
- Allows users to pause auto-playing carousels

## Best Practices

- Provide clear navigation controls for users
- Use appropriate slide transitions and timing
- Ensure carousel content is accessible via keyboard
- Consider providing alternative navigation methods
- Test with assistive technologies for accessibility
- Use consistent spacing and sizing for carousel items
- Provide indicators when showing multiple slides
- Consider performance with large numbers of slides

</DocLayout>
